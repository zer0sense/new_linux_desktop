---
- name: Snap
  become: true
  package:
    name:
      - snapd
    state: present

- name: Link for Classic Snap
  become: yes
  file:
    src: "/var/snapd/snap"
    dest: "/snap"
    state: link
    owner: "{{ user }}"
    group: "{{ user }}"
    mode: 0775